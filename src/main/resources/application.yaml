server:
  port: 8079
  servlet:
    context-path: /financial_assistant
spring:
  datasource:
    driver-class-name: org.h2.Driver
#    url: jdbc:h2:mem:mydatabase;MODE=PostgreSQL;
    url: jdbc:h2:file:C:/Main/Education/GB/fcons;MODE=PostgreSQL;AUTO_SERVER=TRUE;
    username: pk
    password:
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
